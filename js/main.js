/**
 * Main entry point for the endless runner game
 */
document.addEventListener('DOMContentLoaded', () => {
    // Initialize the game
    const game = new Game();
    
    // Log initialization
    console.log('Endless Runner initialized');
});
